<!DOCTYPE html>
<html lang="en">
  <head>
    <title>RailsPokedex</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <h1 class="header"> Rails Pokedex </h1>
    <div class="navbar-container">
      <div class="navbar">
        <ul>
          <li><%= link_to "Home", pokemon_infos_path %></li>
          <li><%= link_to "About", about_path %></li>
          <li><%= link_to "Generations", pokemon_gens_path %></li>
          <li><%= link_to "Types", type_names_path %></li>
          <li><%= link_to "Ranks", pokemon_ranks_path %></li>
      </ul>
      </div>
    </div>
    <div class="search">
      <%= form_with(url:search_pokemon_infos_path, method: :get, local:true) do %>
        <%= text_field_tag(:keywords, nil, class: "search-input", placeholder: "Search pokemon") %>
        <%= collection_select(:search, :gen_id, PokemonGen.all, :id, :name, { prompt: "Select generation" }, class: "search-dropdown") %>
        <%= submit_tag("Search", class: "search-button")%>
      <% end %>     
    </div>
    <%= yield %>
  </body>
</html>
