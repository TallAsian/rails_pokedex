<h1><%= @pokemon_rank.name %> pokemons</h1>
<div>
    <ol class="pokemon-container">
        <% @pokemon_rank.pokemon_info.each do |rank| %>
            <li class="pokemon-name">
                <%= link_to (rank) do %>
                    <%= image_tag "#{rank.id}.png", class: "pokemon-image" %>
                    <%= rank.name %>
                <% end %>
            </li>
        <% end %>
    </ol>
</div>